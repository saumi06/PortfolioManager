import { ColorGeneratorPipe } from './color-generator.pipe';

describe('ColorGeneratorPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorGeneratorPipe();
    expect(pipe).toBeTruthy();
  });
});
